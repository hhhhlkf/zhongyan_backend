# 后端重构计划

版本：v0.2-backend-clean  
来源：基于 `重构计划v0.1.md` 调整为后端清洁重构方案  
日期：2026-04-28

## 1. 定位

本文定义后端清洁重构方案。重构后端不保留原有功能实现，不保留旧接口，不保留旧返回结构，不建立兼容层。现有代码只作为业务线索和设备依赖线索参考，不作为必须兼容的约束。

新后端目标是建设面向无人机遥感应急场景的任务平台，核心模型为：

```text
Mission -> Task -> Command/Event -> Asset
```

其中 `Task` 是系统最小可执行业务单元。采集、处理、传输、坐标计算、预览生成、图层发布、Agent 分析、报告生成都必须建模为可追踪、可重试、可审计、可回放的 Task。

### 1.1 清洁重构边界

明确不保留：

- 不保留现有 Controller 路径，如 `/camera/control`、`/data/select`、`/data/recent`、`/uav/info/realtime`。
- 不保留现有 GET 控制动作风格。
- 不保留现有静态资源目录作为业务数据主存储。
- 不保留现有文件扫描、共享目录读取、UDP 文件存储、SSH 字符串判断等实现方式。
- 不保留现有 `dao/entity`、`service/impl`、`utils/FileUtils` 等代码组织方式。
- 不保留当前 `permitAll` 安全策略。
- 不保留当前模拟数据接口作为正式能力。

可以参考：

- `application.yml` 中已有设备地址、命令、FOV、资源路径，用于梳理初始设备配置。
- 现有 `static/images` 中的样例图片、poly、处理结果，用于准备测试 fixture 或一次性数据导入。
- 现有 SSH、共享目录、UDP 相关代码，用于识别真实设备协议和失败场景。

### 1.2 新后端目标

- 建立统一 Mission/Task/Command/Event/Asset 模型。
- 建立清晰的领域分层、应用服务、端口接口和基础设施适配器。
- 建立正式配置中心，所有设备、相机、模型、传输能力通过版本化配置注册。
- 建立 PostgreSQL/PostGIS 主数据和空间数据存储。
- 建立 MinIO 对象存储，替代仓库内静态目录承载业务数据。
- 建立 Kafka 事件总线，支撑任务事件、设备命令、资产事件、遥测事件和 Agent 审计。
- 建立 Redis 运行态能力，包括分布式锁、在线状态、热点缓存和限流。
- 建立 SSE/WebSocket 实时推送能力。
- 建立 Spring Security + JWT + RBAC。
- 建立统一 Agent Orchestrator 和 Tool Gateway。

## 2. 技术栈

### 2.1 目标后端栈

| 组件 | 版本/选型 | 用途 |
| --- | --- | --- |
| JDK | Java 21 | 目标运行时，支持虚拟线程 |
| Spring Boot | 4.x | 后端框架 |
| Spring Web MVC | 默认 Web 层 | REST API、SSE |
| Spring Security | 随 Boot 版本 | JWT、RBAC、方法权限 |
| Maven Wrapper | 保留 | 构建入口 |
| PostgreSQL | 16+ | 业务主库 |
| PostGIS | 3+ | 空间几何、空间索引 |
| pgvector | 0.7+ | Agent RAG 向量检索 |
| Redis | 7+ | 缓存、锁、在线状态、限流 |
| Kafka | 3.x | 事件总线 |
| MinIO | RELEASE 版 | 对象存储 |
| GeoServer | 2.25+ | WMS/WMTS 图层发布 |
| Spring AI | 与 Boot 版本匹配 | Chat、Embedding、Tool Calling、RAG |
| OpenTelemetry | 稳定版 | 链路追踪 |
| Prometheus/Grafana | 稳定版 | 指标和看板 |

### 2.2 实施基线

如果先在当前仓库内启动第一批重构，可以短期保持 `Spring Boot 2.7.5 + Java 11` 建立包结构和领域边界。但正式目标代码应以 Java 21 和 Spring Boot 4.x 为准。

推荐策略：

1. 先建立全新包结构、领域模型和接口定义。
2. 再升级 Java 21 和 Spring Boot 4.x。
3. 最后接入数据库、Kafka、Redis、MinIO、GeoServer 和 Agent。

## 3. 环境准备

### 3.1 本地开发环境

| 项目 | 要求 |
| --- | --- |
| JDK | Java 21，过渡期可 Java 11 |
| Maven | 使用 `.\mvnw.cmd` |
| Docker | 用于本地依赖服务 |
| 服务端口 | `8088` |
| API 前缀 | `/v2`，只作为新接口上下文前缀，不表示兼容旧接口 |
| Profile | `local`、`dev`、`edge`、`prod`、`test` |

建议命令：

```powershell
.\mvnw.cmd test
.\mvnw.cmd spring-boot:run -Dspring-boot.run.profiles=local
```

### 3.2 本地依赖服务

新增：

```text
deploy/
  docker-compose.local.yml
  docker-compose.observability.yml
  init/
    postgres/
    minio/
    kafka/
src/main/resources/
  application.yml
  application-local.yml
  application-dev.yml
  application-edge.yml
  application-prod.yml
  application-test.yml
```

`docker-compose.local.yml` 至少包含：

- PostgreSQL + PostGIS + pgvector。
- Redis。
- Kafka。
- MinIO。
- GeoServer。
- Prometheus。
- Grafana。

### 3.3 环境变量

| 变量 | 含义 |
| --- | --- |
| `BMS_PROFILE` | 当前运行环境 |
| `BMS_PUBLIC_BASE_URL` | 后端对外访问地址 |
| `BMS_DB_URL` | PostgreSQL 连接地址 |
| `BMS_DB_USERNAME` | 数据库用户名 |
| `BMS_DB_PASSWORD` | 数据库密码 |
| `BMS_REDIS_HOST` | Redis 地址 |
| `BMS_REDIS_PASSWORD` | Redis 密码 |
| `BMS_KAFKA_BOOTSTRAP_SERVERS` | Kafka broker 地址 |
| `BMS_MINIO_ENDPOINT` | MinIO 地址 |
| `BMS_MINIO_ACCESS_KEY` | MinIO access key |
| `BMS_MINIO_SECRET_KEY` | MinIO secret key |
| `BMS_GEOSERVER_URL` | GeoServer 地址 |
| `BMS_GEOSERVER_USERNAME` | GeoServer 用户名 |
| `BMS_GEOSERVER_PASSWORD` | GeoServer 密码 |
| `BMS_JWT_SECRET` | JWT 密钥 |
| `BMS_SECRET_MASTER_KEY` | 本地密钥加密主密钥 |
| `BMS_DEVICE_MODE` | `mock` 或 `real` |
| `BMS_AGENT_BASE_URL` | 本地或云端模型 OpenAI-compatible API 地址 |
| `BMS_AGENT_API_KEY` | 模型 API Key |

### 3.4 真实设备准备

真实设备只在 `BMS_DEVICE_MODE=real` 时启用。

启用前必须登记：

- 设备编号。
- 设备类型。
- 边缘节点编号。
- IP、端口、认证方式。
- SSH、HTTP 或 gRPC 控制协议。
- 健康检查命令或接口。
- 启动命令或接口。
- 停止命令或接口。
- 输出目录或对象存储路径。
- 命令超时。
- 重试策略。
- 风险等级。

真实设备执行规则：

- 所有启动、停止、发布、删除动作必须经 TaskCommand。
- 高风险动作必须审批。
- 所有设备原始输出必须保存为 Attempt 日志。
- 设备不可达时不得阻塞应用线程池。

### 3.5 测试数据准备

新增：

```text
src/test/resources/fixtures/
  images/
  poly/
  telemetry/
  command-output/
  model-result/
```

规则：

- 测试只使用 `fixtures`。
- 历史 `static/images` 可人工挑选样例复制为 fixture，但新测试不直接读写仓库业务目录。
- 集成测试必须可以在 `mock` 设备模式下跑通。

## 4. 文件组织

重构后采用单体内清晰分层。第一版不拆 Maven 多模块，先通过包结构固化边界。

```text
src/main/java/com/zhongyan/uav/
  ZhongyanUavApplication.java

  common/
    api/
      PageQuery.java
      PageView.java
      SortQuery.java
    config/
      AsyncConfig.java
      JacksonConfig.java
      SecurityConfig.java
      ThreadPoolConfig.java
      VirtualThreadConfig.java
    error/
      ErrorCode.java
      BusinessException.java
      ExternalSystemException.java
      GlobalExceptionHandler.java
    response/
      ApiResult.java
      ResponseResult.java
      ResponseResultHandler.java
    security/
      CurrentUser.java
      JwtAuthenticationFilter.java
      JwtTokenService.java
      PermissionCode.java
      RoleCode.java
    util/
      IdGenerator.java
      Jsons.java
      TimeRange.java

  mission/
    api/
      MissionController.java
      request/
      response/
    application/
      MissionApplicationService.java
      MissionQueryService.java
    domain/
      Mission.java
      MissionRegion.java
      MissionStatus.java
      MissionRepository.java
    infrastructure/
      JpaMissionRepository.java

  task/
    api/
      TaskController.java
      TaskCommandController.java
      TaskEventController.java
      TaskApprovalController.java
      request/
      response/
    application/
      TaskApplicationService.java
      TaskCommandApplicationService.java
      TaskApprovalService.java
      TaskQueryService.java
      TaskSchedulerService.java
      TaskRecoveryService.java
    domain/
      Task.java
      TaskType.java
      TaskStatus.java
      TaskAttempt.java
      TaskCommand.java
      TaskCommandStatus.java
      TaskEvent.java
      TaskEventType.java
      TaskDependency.java
      TaskResourceLease.java
      TaskStateMachine.java
      TaskRepository.java
      TaskAttemptRepository.java
      TaskCommandRepository.java
      TaskEventRepository.java
      TaskResourceLeaseRepository.java
    executor/
      TaskExecutor.java
      TaskExecutionContext.java
      TaskExecutionResult.java
      CaptureTaskExecutor.java
      ProcessTaskExecutor.java
      TransferTaskExecutor.java
      GeoBoundaryTaskExecutor.java
      PreviewTaskExecutor.java
      PublishLayerTaskExecutor.java
      DemoPlaybackTaskExecutor.java
      AgentAnalysisTaskExecutor.java
      ReportGenerationTaskExecutor.java
    infrastructure/
      JpaTaskRepository.java
      JpaTaskAttemptRepository.java
      JpaTaskCommandRepository.java
      JpaTaskEventRepository.java
      JpaTaskResourceLeaseRepository.java

  configcenter/
    api/
      DeviceConfigController.java
      CameraConfigController.java
      ModelConfigController.java
      TransferConfigController.java
      ConfigValidationController.java
      request/
      response/
    application/
      DeviceConfigApplicationService.java
      CameraConfigApplicationService.java
      ModelConfigApplicationService.java
      TransferConfigApplicationService.java
      ConfigValidationService.java
    domain/
      DeviceConfig.java
      CameraConfigVersion.java
      ModelConfigVersion.java
      TransferConfigVersion.java
      ModelArtifact.java
      ConfigValidation.java
      ConfigStatus.java
      DeviceConfigRepository.java
      CameraConfigRepository.java
      ModelConfigRepository.java
      TransferConfigRepository.java
      ConfigValidationRepository.java
    infrastructure/
      JpaDeviceConfigRepository.java
      JpaCameraConfigRepository.java
      JpaModelConfigRepository.java
      JpaTransferConfigRepository.java
      JpaConfigValidationRepository.java
      BootstrapConfigImporter.java

  device/
    application/
      DeviceCommandService.java
      DeviceHealthService.java
      DeviceLeaseService.java
    domain/
      DeviceCommandPayload.java
      DeviceCommandResult.java
      DeviceHealth.java
      DeviceProtocol.java
      DeviceRiskLevel.java
    port/
      DeviceCommandExecutor.java
      CameraAdapter.java
      ModelAdapter.java
      TransferAdapter.java
      ClockSyncAdapter.java
    infrastructure/
      ssh/
        SshDeviceCommandExecutor.java
        SshCameraAdapter.java
        SshModelAdapter.java
        SshTransferAdapter.java
      http/
        HttpDeviceCommandExecutor.java
      mock/
        MockCameraAdapter.java
        MockModelAdapter.java
        MockTransferAdapter.java

  asset/
    api/
      AssetController.java
      AssetLayerController.java
      request/
      response/
    application/
      AssetApplicationService.java
      AssetQueryService.java
      PreviewApplicationService.java
    domain/
      Asset.java
      AssetType.java
      AssetRole.java
      AssetStatus.java
      GeoStatus.java
      TaskAsset.java
      AssetRepository.java
      TaskAssetRepository.java
    port/
      AssetStoragePort.java
      PreviewGeneratorPort.java
      ChecksumPort.java
    infrastructure/
      minio/
        MinioAssetStorage.java
      preview/
        JavaImagePreviewGenerator.java
      checksum/
        Sha256ChecksumPort.java

  geo/
    api/
      GeoController.java
      LayerController.java
    application/
      GeoBoundaryApplicationService.java
      LayerPublishApplicationService.java
    domain/
      GeoBoundary.java
      GeoCalculationInput.java
      LayerPublishRecord.java
      LayerStatus.java
      LayerRepository.java
    port/
      GroundElevationPort.java
      GeoServerPort.java
    infrastructure/
      OpenMeteoGroundElevationClient.java
      GeoServerRestClient.java
      JpaLayerRepository.java

  telemetry/
    api/
      UavTelemetryController.java
    application/
      UavTelemetryIngestService.java
      UavTelemetryQueryService.java
    domain/
      UavTelemetry.java
      UavTelemetryRepository.java
    infrastructure/
      UdpTelemetryReceiver.java
      KafkaTelemetryConsumer.java
      JpaUavTelemetryRepository.java

  event/
    domain/
      DomainEvent.java
      EventEnvelope.java
      EventType.java
    port/
      EventPublisher.java
      EventSubscriber.java
      OutboxRepository.java
    application/
      OutboxPublishService.java
      EventReplayService.java
    infrastructure/
      KafkaEventPublisher.java
      KafkaEventConsumer.java
      JpaOutboxRepository.java

  realtime/
    api/
      RealtimeController.java
    application/
      RealtimePushService.java
    infrastructure/
      SseRealtimePushService.java
      WebSocketRealtimePushService.java

  agent/
    api/
      AgentController.java
      AgentToolController.java
      request/
      response/
    application/
      AgentApplicationService.java
      ToolGatewayService.java
      RagService.java
      ReportDraftService.java
    domain/
      AgentSession.java
      AgentMessage.java
      AgentToolCall.java
      ToolRiskLevel.java
      AgentSessionRepository.java
      AgentMessageRepository.java
      AgentToolCallRepository.java
    port/
      ChatModelPort.java
      EmbeddingPort.java
      VectorStorePort.java
      AgentTool.java
    infrastructure/
      SpringAiChatModelAdapter.java
      PgVectorStoreAdapter.java
      JpaAgentSessionRepository.java
      JpaAgentMessageRepository.java
      JpaAgentToolCallRepository.java

src/main/resources/
  application.yml
  application-local.yml
  application-dev.yml
  application-edge.yml
  application-prod.yml
  application-test.yml
  db/migration/
    V1__core_mission_task_asset.sql
    V2__config_center.sql
    V3__telemetry_event_outbox.sql
    V4__agent.sql
```

### 4.1 删除旧组织方式

目标结构中不再保留：

- `controller` 根包。
- `service` 根包。
- `service.impl` 根包。
- `dao.entity` 作为通用实体包。
- `utils` 作为业务工具堆放包。
- `legacy` 包。

旧类处理方式：

| 旧类/旧包 | 处理 |
| --- | --- |
| `DataController` | 删除，功能按新 Task/Asset API 重新定义 |
| `DeviceController` | 删除，功能按 Device/Task API 重新定义 |
| `EmergencyController` | 删除，功能按 Mission/Asset API 重新定义 |
| `CollAndProcController` | 删除，功能按 Task API 重新定义 |
| `Process2Controller` | 删除，功能按 Task/Asset/Layer API 重新定义 |
| `UavController` | 删除，功能按 Telemetry API 重新定义 |
| `CameraServiceImpl` | 删除，重新实现 `CameraAdapter` |
| `MethodServiceImpl` | 删除，重新实现 `ModelAdapter` |
| `TransferServiceImpl` | 删除，重新实现 `TransferAdapter` |
| `DataManagerServiceImpl` | 删除，重新实现 `AssetApplicationService` |
| `FileUtils` | 删除，拆分为存储、URL、校验、预览等端口实现 |
| `UdpReceiverService` | 删除，重新实现 `UdpTelemetryReceiver` |

## 5. 分层设计

后端采用端口适配器架构。

| 层 | 包 | 职责 | 依赖规则 |
| --- | --- | --- | --- |
| API 层 | `*.api` | Controller、请求 DTO、响应 DTO、参数校验 | 只依赖 application 和 common |
| 应用层 | `*.application` | 用例编排、事务边界、权限校验、事件触发 | 依赖 domain 和 port |
| 领域层 | `*.domain` | 聚合、状态机、领域规则、仓储接口 | 不依赖 Spring、HTTP、DB、Kafka、文件系统 |
| 执行层 | `task.executor` | Task 执行策略 | 依赖 domain 和端口接口 |
| 端口层 | `*.port` | 外部能力抽象 | 只定义接口和模型 |
| 基础设施层 | `*.infrastructure` | JPA、Kafka、Redis、MinIO、GeoServer、SSH、HTTP、UDP | 实现端口 |
| 通用层 | `common.*` | 错误、响应、安全、配置、工具 | 不包含业务流程 |

禁止事项：

- Controller 直接执行 SSH、文件复制、目录扫描、坐标计算。
- Application Service 直接调用 `JSch`、`KafkaTemplate`、`MinioClient`、`GeoServer REST`。
- Domain 对象依赖 Spring 注解、HTTP request、JPA 注解。
- Task 执行期间读取最新配置替代配置快照。
- 设备命令绕过 TaskCommand。
- Agent 工具绕过 Tool Gateway。

## 6. 核心领域模型

### 6.1 Mission

Mission 表示一次完整应急任务、外场作业、演示任务或算法验证流程。

字段：

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `missionId` | `String` | 唯一编号 |
| `name` | `String` | 名称 |
| `scenarioType` | `String` | `emergency`、`demo`、`training`、`test` |
| `region` | `MissionRegion` | 行政区、中心点、空间范围 |
| `status` | `MissionStatus` | 状态 |
| `priority` | `int` | 优先级 |
| `createdBy` | `String` | 创建人 |
| `createdAt` | `Instant` | 创建时间 |
| `startedAt` | `Instant` | 开始时间 |
| `endedAt` | `Instant` | 结束时间 |
| `description` | `String` | 说明 |

状态：

- `DRAFT`
- `ACTIVE`
- `PAUSED`
- `COMPLETED`
- `CANCELLED`
- `FAILED`

### 6.2 Task

Task 是最小可执行业务单元。

字段：

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `taskId` | `String` | 唯一编号 |
| `missionId` | `String` | 所属 Mission |
| `taskType` | `TaskType` | 任务类型 |
| `status` | `TaskStatus` | 当前状态 |
| `priority` | `int` | 优先级 |
| `deviceId` | `String` | 目标设备 |
| `modelId` | `String` | 目标模型 |
| `configSnapshot` | `JsonNode` | 执行配置快照 |
| `inputAssetIds` | `List<String>` | 输入资产 |
| `outputAssetIds` | `List<String>` | 输出资产 |
| `progress` | `BigDecimal` | 进度 |
| `createdBy` | `String` | 创建人或 Agent |
| `createdAt` | `Instant` | 创建时间 |
| `updatedAt` | `Instant` | 更新时间 |
| `startedAt` | `Instant` | 开始时间 |
| `endedAt` | `Instant` | 结束时间 |
| `errorCode` | `String` | 错误码 |
| `errorMessage` | `String` | 错误信息 |

任务类型：

- `CAPTURE`
- `PROCESS`
- `TRANSFER`
- `CALCULATE_GEO_BOUNDARY`
- `GENERATE_PREVIEW`
- `PUBLISH_LAYER`
- `DEMO_PLAYBACK`
- `AGENT_ANALYSIS`
- `REPORT_GENERATION`

状态：

- `DRAFT`
- `WAITING_APPROVAL`
- `QUEUED`
- `DISPATCHING`
- `RUNNING`
- `WAITING_ASSET`
- `POST_PROCESSING`
- `COMPLETED`
- `FAILED`
- `RETRYING`
- `CANCELLED`
- `TIMEOUT`

### 6.3 TaskAttempt

记录 Task 每次执行尝试。

字段：

| 字段 | 说明 |
| --- | --- |
| `attemptId` | 尝试编号 |
| `taskId` | 所属 Task |
| `attemptNo` | 第几次尝试 |
| `executorNode` | 执行节点 |
| `startedAt` | 开始时间 |
| `endedAt` | 结束时间 |
| `result` | 执行结果 |
| `errorCode` | 错误码 |
| `errorMessage` | 错误信息 |
| `rawLogObjectKey` | 原始日志对象存储 key |

规则：

- 每次真正开始执行 Task 创建一条 Attempt。
- 重试不覆盖旧 Attempt。
- 设备输出、模型日志、异常堆栈摘要都绑定 Attempt。

### 6.4 TaskCommand

表示一次对 Task 或设备的命令请求。

字段：

| 字段 | 说明 |
| --- | --- |
| `commandId` | 命令编号 |
| `taskId` | 关联 Task |
| `missionId` | 关联 Mission |
| `deviceId` | 目标设备 |
| `commandType` | 命令类型 |
| `payload` | 参数 JSON |
| `idempotencyKey` | 幂等键 |
| `requestedBy` | 请求来源 |
| `riskLevel` | 风险等级 |
| `requiresApproval` | 是否需要审批 |
| `approvedBy` | 审批人 |
| `approvedAt` | 审批时间 |
| `status` | 命令状态 |
| `createdAt` | 创建时间 |
| `dispatchedAt` | 下发时间 |
| `completedAt` | 完成时间 |

命令类型：

- `START`
- `STOP`
- `PAUSE`
- `RESUME`
- `RETRY`
- `CANCEL`
- `CONFIRM`
- `ROLLBACK`

高风险命令：

- 启动采集。
- 停止设备。
- 启动处理。
- 停止处理。
- 启动传输。
- 删除资产。
- 覆盖资产。
- 发布图层。
- 修改设备配置。
- 修改模型配置。

### 6.5 TaskEvent

记录 Task 生命周期事件。

字段：

| 字段 | 说明 |
| --- | --- |
| `eventId` | 事件编号 |
| `taskId` | 关联 Task |
| `missionId` | 关联 Mission |
| `eventType` | 事件类型 |
| `statusBefore` | 变更前状态 |
| `statusAfter` | 变更后状态 |
| `payload` | 事件 JSON |
| `source` | 事件来源 |
| `createdAt` | 发生时间 |

事件类型：

- `CREATED`
- `QUEUED`
- `COMMAND_CREATED`
- `COMMAND_SENT`
- `RUNNING`
- `PROGRESS_CHANGED`
- `ASSET_CREATED`
- `FAILED`
- `RETRYING`
- `COMPLETED`
- `CANCELLED`
- `APPROVAL_REQUIRED`
- `APPROVED`
- `REJECTED`
- `TIMEOUT`

规则：

- 所有状态变化必须产生 TaskEvent。
- TaskEvent 写数据库，并通过 outbox 投递 Kafka。
- 前端只通过新实时接口感知事件。

### 6.6 Asset

表示可管理数据资产。

字段：

| 字段 | 说明 |
| --- | --- |
| `assetId` | 资产编号 |
| `assetType` | 资产类型 |
| `status` | 资产状态 |
| `name` | 名称 |
| `objectKey` | MinIO 对象 key |
| `previewObjectKey` | 预览对象 key |
| `geometry` | PostGIS geometry |
| `layerUrl` | GeoServer 图层地址 |
| `sourceTaskId` | 来源 Task |
| `tags` | 标签 |
| `metadata` | 元数据 JSON |
| `geoStatus` | 坐标状态 |
| `createdAt` | 创建时间 |

资产类型：

- `IMAGE`
- `GEOTIFF`
- `VIDEO`
- `PDF`
- `DOCUMENT`
- `MODEL_RESULT`
- `LOG`
- `REPORT`
- `LAYER`

## 7. 核心接口和类

### 7.1 Mission 模块

| 类型 | 名称 | 职责 |
| --- | --- | --- |
| Controller | `MissionController` | 创建、启动、暂停、取消、查询 Mission |
| Application | `MissionApplicationService` | Mission 生命周期命令 |
| Application | `MissionQueryService` | Mission 查询 |
| Domain | `Mission` | Mission 聚合根 |
| Enum | `MissionStatus` | Mission 状态 |
| Repository Port | `MissionRepository` | Mission 持久化端口 |
| DTO | `CreateMissionRequest` | 创建 Mission 请求 |
| DTO | `MissionView` | Mission 响应 |

接口草案：

```java
public interface MissionApplicationService {
    MissionView create(CreateMissionRequest request, CurrentUser user);
    MissionView start(String missionId, CurrentUser user);
    MissionView pause(String missionId, CurrentUser user);
    MissionView cancel(String missionId, CancelMissionRequest request, CurrentUser user);
}
```

### 7.2 Task 模块

| 类型 | 名称 | 职责 |
| --- | --- | --- |
| Controller | `TaskController` | Task 创建、提交、取消、查询 |
| Controller | `TaskCommandController` | Task 命令创建和查询 |
| Controller | `TaskEventController` | Task 事件查询 |
| Controller | `TaskApprovalController` | 高风险命令审批 |
| Application | `TaskApplicationService` | Task 用例编排 |
| Application | `TaskCommandApplicationService` | 命令幂等、风险判断、下发 |
| Application | `TaskApprovalService` | 审批通过和拒绝 |
| Application | `TaskSchedulerService` | Task 入队和调度 |
| Application | `TaskRecoveryService` | 超时、失败、锁过期恢复 |
| Domain Service | `TaskStateMachine` | 状态流转校验 |
| Executor | `TaskExecutor` | Task 执行器统一接口 |

接口草案：

```java
public interface TaskApplicationService {
    TaskView createTask(String missionId, CreateTaskRequest request, CurrentUser user);
    TaskView submit(String taskId, CurrentUser user);
    TaskView retry(String taskId, RetryTaskRequest request, CurrentUser user);
    TaskView cancel(String taskId, CancelTaskRequest request, CurrentUser user);
}

public interface TaskCommandApplicationService {
    TaskCommandView createCommand(String taskId, CreateTaskCommandRequest request, CurrentUser user);
    TaskCommandView dispatch(String commandId);
}

public interface TaskExecutor {
    boolean supports(TaskType taskType);
    TaskExecutionResult execute(TaskExecutionContext context);
}
```

### 7.3 Config Center 模块

| 类型 | 名称 | 职责 |
| --- | --- | --- |
| Controller | `DeviceConfigController` | 设备注册、查询、禁用 |
| Controller | `CameraConfigController` | 相机配置版本 |
| Controller | `ModelConfigController` | 模型配置版本 |
| Controller | `TransferConfigController` | 传输配置版本 |
| Controller | `ConfigValidationController` | 配置验证查询 |
| Application | `ConfigValidationService` | 连通性、命令 dry-run、目录检查、测试样例验证 |
| Domain | `DeviceConfig` | 通用设备配置 |
| Domain | `CameraConfigVersion` | 相机配置版本 |
| Domain | `ModelConfigVersion` | 模型配置版本 |
| Domain | `TransferConfigVersion` | 传输配置版本 |
| Infrastructure | `BootstrapConfigImporter` | 一次性导入旧配置线索，不提供运行时兼容 |

规则：

- 所有设备、相机、模型、传输配置必须先注册，再被 Task 引用。
- 每次修改命令、路径、FOV、认证方式、模型参数都创建新版本。
- 只有 `READY` 版本可以创建正式 Task。
- Task 创建时复制配置版本快照到 `task.config_snapshot`。

### 7.4 Device 模块

| 类型 | 名称 | 职责 |
| --- | --- | --- |
| Port | `DeviceCommandExecutor` | 执行通用设备命令 |
| Port | `CameraAdapter` | 相机检查、启动、停止 |
| Port | `ModelAdapter` | 模型检查、启动、停止 |
| Port | `TransferAdapter` | 传输检查、启动、停止 |
| Port | `ClockSyncAdapter` | 远程时间同步 |
| Infrastructure | `SshDeviceCommandExecutor` | SSH 设备执行 |
| Infrastructure | `HttpDeviceCommandExecutor` | HTTP 设备执行 |
| Infrastructure | `MockCameraAdapter` | 本地模拟相机 |
| Infrastructure | `MockModelAdapter` | 本地模拟模型 |
| Infrastructure | `MockTransferAdapter` | 本地模拟传输 |

接口草案：

```java
public interface DeviceCommandExecutor {
    DeviceCommandResult execute(DeviceCommandPayload payload);
}

public interface CameraAdapter {
    DeviceHealth check(CameraConfigSnapshot config);
    DeviceCommandResult start(CameraConfigSnapshot config, CaptureParameters parameters);
    DeviceCommandResult stop(CameraConfigSnapshot config);
}

public interface ModelAdapter {
    DeviceHealth check(ModelConfigSnapshot config);
    DeviceCommandResult start(ModelConfigSnapshot config, ProcessParameters parameters);
    DeviceCommandResult stop(ModelConfigSnapshot config);
}

public interface TransferAdapter {
    DeviceHealth check(TransferConfigSnapshot config);
    DeviceCommandResult start(TransferConfigSnapshot config, TransferParameters parameters);
    DeviceCommandResult stop(TransferConfigSnapshot config);
}
```

### 7.5 Asset 模块

| 类型 | 名称 | 职责 |
| --- | --- | --- |
| Controller | `AssetController` | 资产创建、查询、删除、统计 |
| Controller | `AssetLayerController` | 图层发布和查询 |
| Application | `AssetApplicationService` | 资产创建、绑定 Task、元数据写入 |
| Application | `AssetQueryService` | 资产检索 |
| Application | `PreviewApplicationService` | 预览生成 |
| Port | `AssetStoragePort` | 对象存储抽象 |
| Port | `PreviewGeneratorPort` | 预览生成抽象 |
| Infrastructure | `MinioAssetStorage` | MinIO 存储 |
| Infrastructure | `JavaImagePreviewGenerator` | Java 图片预览生成 |

接口草案：

```java
public interface AssetStoragePort {
    StoredObject put(AssetObject object);
    Optional<StoredObject> get(String objectKey);
    void delete(String objectKey);
}

public interface PreviewGeneratorPort {
    PreviewResult generate(PreviewRequest request);
}
```

### 7.6 Geo 模块

| 类型 | 名称 | 职责 |
| --- | --- | --- |
| Controller | `GeoController` | 坐标计算任务入口 |
| Controller | `LayerController` | 图层查询和发布入口 |
| Application | `GeoBoundaryApplicationService` | 四角坐标计算 |
| Application | `LayerPublishApplicationService` | GeoServer 发布编排 |
| Port | `GroundElevationPort` | 高程查询 |
| Port | `GeoServerPort` | GeoServer 发布 |
| Infrastructure | `OpenMeteoGroundElevationClient` | 在线高程查询 |
| Infrastructure | `GeoServerRestClient` | GeoServer REST |

规则：

- 坐标计算失败不阻塞资产入库，但资产 `geoStatus=FAILED`。
- 坐标计算保存输入快照，支持复算。
- 图层发布必须审批。

### 7.7 Telemetry 模块

| 类型 | 名称 | 职责 |
| --- | --- | --- |
| Controller | `UavTelemetryController` | 遥测写入和查询 |
| Application | `UavTelemetryIngestService` | 遥测接收 |
| Application | `UavTelemetryQueryService` | 最新遥测、时间范围、轨迹查询 |
| Domain | `UavTelemetry` | 遥测记录 |
| Infrastructure | `UdpTelemetryReceiver` | UDP 接收 |
| Infrastructure | `KafkaTelemetryConsumer` | Kafka 遥测消费 |
| Infrastructure | `JpaUavTelemetryRepository` | 数据库存储 |

### 7.8 Event 与 Realtime 模块

| 类型 | 名称 | 职责 |
| --- | --- | --- |
| Port | `EventPublisher` | 领域事件发布 |
| Port | `EventSubscriber` | 领域事件订阅 |
| Port | `OutboxRepository` | 事务 outbox |
| Application | `OutboxPublishService` | outbox 投递 |
| Application | `EventReplayService` | 事件回放 |
| Infrastructure | `KafkaEventPublisher` | Kafka 发布 |
| Infrastructure | `KafkaEventConsumer` | Kafka 消费 |
| Application | `RealtimePushService` | 实时推送 |
| Infrastructure | `SseRealtimePushService` | SSE |
| Infrastructure | `WebSocketRealtimePushService` | WebSocket |

Topic：

| Topic | Key | 用途 |
| --- | --- | --- |
| `task-events` | `taskId` | Task 生命周期 |
| `device-commands` | `deviceId` | 设备命令 |
| `asset-events` | `assetId` | 资产事件 |
| `uav-telemetry` | `uavId` | 遥测 |
| `agent-events` | `sessionId` | Agent 事件 |
| `dead-letter-events` | 原始 key | 死信 |

### 7.9 Agent 模块

| 类型 | 名称 | 职责 |
| --- | --- | --- |
| Controller | `AgentController` | 会话、消息、事件 |
| Controller | `AgentToolController` | 工具调用和审批 |
| Application | `AgentApplicationService` | 对话编排 |
| Application | `ToolGatewayService` | 工具权限、风险、审计 |
| Application | `RagService` | 检索增强 |
| Application | `ReportDraftService` | 报告草稿 |
| Port | `ChatModelPort` | 模型调用 |
| Port | `EmbeddingPort` | 向量化 |
| Port | `VectorStorePort` | 向量检索 |
| Port | `AgentTool` | 可调用工具 |

Agent 规则：

- 查询、总结、统计可自动执行。
- 启动、停止、删除、发布、覆盖必须审批。
- 所有工具调用写入 `agent_tool_call`。
- Agent 只能通过 Tool Gateway 创建 TaskCommand。

## 8. API 定义

所有外部访问路径带 `/v2`，但这些都是新接口，不兼容旧路径。

### 8.1 Mission API

```http
POST   /v2/missions
GET    /v2/missions
GET    /v2/missions/{missionId}
POST   /v2/missions/{missionId}/start
POST   /v2/missions/{missionId}/pause
POST   /v2/missions/{missionId}/cancel
GET    /v2/missions/{missionId}/timeline
GET    /v2/missions/{missionId}/summary
```

### 8.2 Task API

```http
POST   /v2/missions/{missionId}/tasks
GET    /v2/missions/{missionId}/tasks
GET    /v2/tasks/{taskId}
POST   /v2/tasks/{taskId}/submit
POST   /v2/tasks/{taskId}/commands
POST   /v2/tasks/{taskId}/retry
POST   /v2/tasks/{taskId}/cancel
GET    /v2/tasks/{taskId}/events
GET    /v2/tasks/{taskId}/attempts
GET    /v2/tasks/{taskId}/assets
```

### 8.3 Approval API

```http
GET    /v2/approvals/pending
GET    /v2/approvals/{commandId}
POST   /v2/approvals/{commandId}/approve
POST   /v2/approvals/{commandId}/reject
```

### 8.4 Configuration API

```http
POST   /v2/config/devices
GET    /v2/config/devices
GET    /v2/config/devices/{deviceId}
POST   /v2/config/devices/{deviceId}/validate
POST   /v2/config/devices/{deviceId}/disable

POST   /v2/config/cameras
GET    /v2/config/cameras
GET    /v2/config/cameras/{cameraConfigId}
POST   /v2/config/cameras/{cameraConfigId}/versions
POST   /v2/config/cameras/{cameraConfigId}/validate
POST   /v2/config/cameras/{cameraConfigId}/activate
POST   /v2/config/cameras/{cameraConfigId}/disable

POST   /v2/config/models
GET    /v2/config/models
GET    /v2/config/models/{modelConfigId}
POST   /v2/config/models/{modelConfigId}/versions
POST   /v2/config/models/{modelConfigId}/artifacts
POST   /v2/config/models/{modelConfigId}/validate
POST   /v2/config/models/{modelConfigId}/activate
POST   /v2/config/models/{modelConfigId}/disable

POST   /v2/config/transfers
GET    /v2/config/transfers
GET    /v2/config/transfers/{transferConfigId}
POST   /v2/config/transfers/{transferConfigId}/versions
POST   /v2/config/transfers/{transferConfigId}/validate
POST   /v2/config/transfers/{transferConfigId}/activate
POST   /v2/config/transfers/{transferConfigId}/disable

GET    /v2/config/validations/{validationId}
GET    /v2/config/validations?configType={type}&configId={id}
```

### 8.5 Asset API

```http
POST   /v2/assets
GET    /v2/assets
GET    /v2/assets/{assetId}
POST   /v2/assets/{assetId}/preview
POST   /v2/assets/{assetId}/geo-boundary
POST   /v2/assets/{assetId}/publish-layer
DELETE /v2/assets/{assetId}
GET    /v2/assets/stats
```

### 8.6 Telemetry API

```http
POST   /v2/uavs/{uavId}/telemetry
GET    /v2/uavs/{uavId}/telemetry/latest
GET    /v2/uavs/{uavId}/telemetry?from={time}&to={time}
GET    /v2/uavs/{uavId}/track?missionId={missionId}
```

### 8.7 Realtime API

```http
GET    /v2/realtime/missions/{missionId}/events
GET    /v2/realtime/tasks/{taskId}/events
GET    /v2/realtime/uavs/{uavId}/telemetry
GET    /v2/realtime/agent/sessions/{sessionId}/events
```

### 8.8 Agent API

```http
POST   /v2/agent/sessions
GET    /v2/agent/sessions/{sessionId}
POST   /v2/agent/sessions/{sessionId}/messages
GET    /v2/agent/sessions/{sessionId}/events
GET    /v2/agent/tool-calls/{toolCallId}
POST   /v2/agent/tool-calls/{toolCallId}/approve
POST   /v2/agent/tool-calls/{toolCallId}/reject
```

## 9. DTO 规范

命名规则：

- 请求对象使用 `*Request`。
- 响应视图使用 `*View`。
- 查询条件使用 `*Query`。
- 内部命令对象使用 `*Command`。
- 不暴露 JPA Entity 和 Domain 聚合。

核心 DTO：

| DTO | 字段 |
| --- | --- |
| `CreateMissionRequest` | `name`、`scenarioType`、`region`、`priority`、`description` |
| `MissionView` | `missionId`、`name`、`status`、`progress`、`createdAt`、`startedAt`、`endedAt` |
| `CreateTaskRequest` | `taskType`、`priority`、`deviceId`、`modelId`、`configRef`、`inputAssetIds`、`parameters` |
| `TaskView` | `taskId`、`missionId`、`taskType`、`status`、`progress`、`errorCode`、`errorMessage` |
| `CreateTaskCommandRequest` | `commandType`、`payload`、`idempotencyKey`、`reason` |
| `TaskCommandView` | `commandId`、`taskId`、`commandType`、`status`、`requiresApproval` |
| `TaskEventView` | `eventId`、`taskId`、`eventType`、`statusBefore`、`statusAfter`、`payload`、`createdAt` |
| `AssetView` | `assetId`、`assetType`、`status`、`name`、`url`、`previewUrl`、`geoStatus`、`layerUrl` |
| `DeviceConfigView` | `deviceId`、`deviceName`、`deviceType`、`host`、`status`、`updatedAt` |
| `CameraConfigVersionView` | `cameraConfigId`、`version`、`cameraType`、`status`、`fov` |
| `ModelConfigVersionView` | `modelConfigId`、`version`、`modelType`、`runtimeType`、`status` |

## 10. 数据库设计

### 10.1 主表

| 表 | 聚合/实体 |
| --- | --- |
| `mission` | Mission |
| `task` | Task |
| `task_attempt` | TaskAttempt |
| `task_command` | TaskCommand |
| `task_event` | TaskEvent |
| `task_dependency` | TaskDependency |
| `task_resource_lease` | TaskResourceLease |
| `asset` | Asset |
| `task_asset` | TaskAsset |
| `device_config` | DeviceConfig |
| `camera_config` | CameraConfigVersion |
| `model_config` | ModelConfigVersion |
| `transfer_config` | TransferConfigVersion |
| `model_artifact` | ModelArtifact |
| `config_validation` | ConfigValidation |
| `uav_telemetry` | UavTelemetry |
| `event_outbox` | EventEnvelope |
| `agent_session` | AgentSession |
| `agent_message` | AgentMessage |
| `agent_tool_call` | AgentToolCall |

### 10.2 存储规则

- PostgreSQL 保存业务主数据。
- PostGIS 保存空间几何和空间索引。
- MinIO 保存图片、视频、日志、报告、模型权重、附件。
- Redis 不作为最终数据源，只保存运行态缓存和锁。
- Kafka 不保存主数据，只承载事件流。
- 历史静态资源如需迁入，必须通过一次性导入任务生成 Asset，不提供运行时扫描兼容。

## 11. 事件与异步

事件写入流程：

1. 应用层执行用例。
2. 同事务写业务表和 `event_outbox`。
3. outbox publisher 投递 Kafka。
4. 投递成功后标记 outbox 已发送。
5. realtime 模块消费事件并推送前端。

幂等规则：

- `TaskCommand.idempotencyKey` 全局唯一。
- Kafka consumer 根据 `eventId` 去重。
- 同一 `deviceId` 的命令使用 Kafka key 保证顺序。
- Task 重试创建新 `TaskAttempt`。

## 12. 安全与审批

### 12.1 安全模型

必须启用：

- JWT 认证。
- RBAC 授权。
- 方法权限。
- API 审计日志。
- 高风险动作审批。

角色：

| 角色 | 权限 |
| --- | --- |
| `ADMIN` | 系统配置、用户、所有任务 |
| `OPERATOR` | 创建任务、启动/停止采集和处理 |
| `ANALYST` | 查询资产、报告、Agent 分析 |
| `VIEWER` | 只读查看 |
| `AGENT` | 只能通过 Tool Gateway 创建待确认命令 |

### 12.2 高风险审批

必须审批：

- 启动采集。
- 停止设备。
- 启动模型处理。
- 停止模型处理。
- 启动传输。
- 删除资产。
- 覆盖资产。
- 发布图层。
- 修改设备命令。
- 修改模型命令。
- Agent 发起的任何写操作。

## 13. 实施阶段

### Phase 0：清洁重构准备

目标：

- 冻结旧接口，不再新增旧接口能力。
- 完成新后端包结构和 API 设计。
- 输出旧代码删除清单。
- 输出设备、路径、命令、数据样例清单。

输出：

- `后端重构计划.md`。
- 新 API 清单。
- 新包结构清单。
- 旧代码删除清单。

### Phase 1：工程骨架和领域模型

目标：

- 建立新包结构。
- 建立 Mission/Task/Command/Event/Asset 领域模型。
- 建立 Task 状态机。
- 建立 repository port。
- 建立 mock repository 和 mock device adapter。
- 建立统一错误码和响应结构。

输出：

- 新 Controller 空壳和 DTO。
- 应用服务接口。
- 领域模型。
- 单元测试。

### Phase 2：数据库和配置中心

目标：

- 接入 PostgreSQL/PostGIS/pgvector。
- 接入 Flyway 或 Liquibase。
- 建立配置中心。
- 将旧 `application.yml` 中设备线索通过一次性导入器转为配置版本。
- Task 创建时生成配置快照。

输出：

- migration。
- Configuration API。
- repository JPA 实现。
- 配置验证任务。

### Phase 3：设备执行和任务调度

目标：

- 实现 Device Adapter。
- 实现 TaskExecutor。
- 实现资源锁。
- 实现 Attempt 日志。
- 实现命令幂等。

输出：

- `CAPTURE`、`PROCESS`、`TRANSFER` executor。
- SSH/HTTP/mock adapter。
- 设备命令审计。

### Phase 4：事件总线和实时推送

目标：

- 接入 outbox。
- 接入 Kafka。
- 接入 Redis。
- 建立 SSE/WebSocket。
- 实现 DLT 和事件回放。

输出：

- Kafka topic。
- Producer/Consumer。
- Realtime API。
- DLT 诊断 API。

### Phase 5：资产和 GIS

目标：

- 接入 MinIO。
- 接入 GeoServer。
- 实现资产入库。
- 实现预览生成。
- 实现坐标计算。
- 实现图层发布。

输出：

- Asset API。
- `GENERATE_PREVIEW` executor。
- `CALCULATE_GEO_BOUNDARY` executor。
- `PUBLISH_LAYER` executor。

### Phase 6：Agent 和报告

目标：

- 接入 Spring AI。
- 建立统一 Agent Orchestrator。
- 建立 Tool Gateway。
- 建立 RAG。
- 建立报告生成 Task。

输出：

- Agent API。
- Agent tool audit。
- `AGENT_ANALYSIS` executor。
- `REPORT_GENERATION` executor。

### Phase 7：观测、压测和上线

目标：

- 接入 Prometheus/Grafana。
- 接入 OpenTelemetry。
- 完成限流、熔断、超时、重试策略。
- 完成大文件、高频遥测、弱网、设备离线压测。
- 删除旧 Controller、Service、Utils 和静态业务数据运行依赖。

输出：

- 监控面板。
- 压测报告。
- 故障演练报告。
- 旧代码移除 PR。

## 14. 测试策略

| 测试类型 | 覆盖内容 |
| --- | --- |
| Unit Test | 状态机、领域规则、DTO 校验、配置快照 |
| Application Test | Mission/Task/Command/Approval 用例 |
| Adapter Test | SSH mock、HTTP mock、MinIO、GeoServer、UDP |
| Repository Test | PostgreSQL、PostGIS、pgvector、outbox |
| Integration Test | Task 执行链路、Kafka、Redis、MinIO、GeoServer |
| Security Test | JWT、RBAC、审批绕过防护 |
| Device Dry-run Test | 真实设备连通性和命令 dry-run |
| E2E Test | Mission -> Capture -> Process -> Transfer -> Geo -> Layer -> Report |

本地最小验证：

```powershell
.\mvnw.cmd test
```

真实设备、共享目录、UDP、GeoServer、MinIO、Kafka 相关验证必须在提交说明中写明验证环境和未验证项。

## 15. 验收标准

### 15.1 Phase 1 验收

- 新包结构建立完成。
- 无 `legacy` 包。
- 新 Controller 不暴露旧路径。
- Task 状态机测试通过。
- Mock 设备模式可跑通任务创建、提交、失败、重试、取消。

### 15.2 v0.2 后端验收

- Mission/Task/Command/Event/Asset 主链路可落库、查询、回放。
- 采集、处理、传输、预览、坐标、图层发布都能以 Task 执行。
- TaskAttempt 和 TaskEvent 完整记录执行过程。
- 设备命令有幂等、原始输出、错误码、超时和重试记录。
- 资产存 MinIO，元数据存 PostgreSQL，空间数据存 PostGIS。
- 前端通过新 SSE/WebSocket 接口获得事件。
- 高风险动作必须审批。
- Agent 不能绕过 Tool Gateway。
- 旧接口不存在。
- 旧代码组织不存在。
- 运行态不依赖 `src/main/resources/static/images` 作为业务数据目录。

## 16. 明确不做

- 不做旧接口兼容。
- 不做旧返回结构兼容。
- 不做旧功能平移。
- 不建立 `legacy` 包。
- 不保留 `controller`、`service.impl`、`dao.entity`、`utils` 的旧组织方式。
- 不让仓库静态资源目录继续承载正式业务数据。
- 不允许 Agent 直接执行高风险动作。
- 不允许设备命令绕过 TaskCommand。
- 不允许领域层依赖 Spring、JPA、Kafka、MinIO、JSch。
