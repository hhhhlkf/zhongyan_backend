# 3.12 安全、审计和上线强化验收

## 验收范围

- JWT 登录与 Bearer Token 鉴权。
- RBAC 与方法权限。
- API 审计日志与 `X-Request-Id`。
- API 限流。
- Actuator、Prometheus 指标。
- GeoServer REST 调用超时、重试、熔断。
- HTTP/SSH 真实设备命令超时、失败分类、熔断保护。
- 压测、弱网、设备离线演练。

## 最小验证命令

```powershell
.\mvnw.cmd test
```

## Mock 环境冒烟

启动服务：

```powershell
.\mvnw.cmd spring-boot:run
```

获取 token：

```powershell
$tokenResponse = Invoke-RestMethod -Method Post -Uri "http://localhost:8088/v2/auth/token" `
  -ContentType "application/json" `
  -Body (@{ username = "admin"; password = "admin" } | ConvertTo-Json)
$token = $tokenResponse.data.accessToken
```

验证健康检查和指标：

```powershell
Invoke-RestMethod "http://localhost:8088/v2/actuator/health"
Invoke-WebRequest "http://localhost:8088/v2/actuator/prometheus" -Headers @{ Authorization = "Bearer $token" }
```

验证鉴权：

```powershell
Invoke-WebRequest "http://localhost:8088/v2/missions" -SkipHttpErrorCheck
Invoke-WebRequest "http://localhost:8088/v2/missions" -Headers @{ Authorization = "Bearer $token" }
```

验证限流，默认同一 `principal + remoteAddr` 每 60 秒 120 次：

```powershell
1..140 | ForEach-Object {
  Invoke-WebRequest "http://localhost:8088/v2/missions" `
    -Headers @{ Authorization = "Bearer $token" } `
    -SkipHttpErrorCheck | Select-Object StatusCode, Content
}
```

预期：超过阈值后返回统一响应，`code=429`。

## Local 真实依赖验收

启动本地依赖：

```powershell
docker compose -f deploy/docker-compose.local.yml up -d
.\mvnw.cmd spring-boot:run -Dspring-boot.run.profiles=local
```

GeoServer 验收项：

- `bms.geo.geoserver.connect-timeout-ms` 生效。
- `bms.geo.geoserver.read-timeout-ms` 生效。
- GeoServer 短暂不可达时重试，连续失败达到 `circuit-failure-threshold` 后熔断。
- 熔断期间图层发布失败必须返回业务错误，不阻塞应用线程。
- 恢复等待 `circuit-open-duration-ms` 后再次调用可进入半恢复路径。

Kafka / MinIO / PostgreSQL 验收项：

- `local` profile 能启动。
- migration 能执行。
- outbox、资产存储、图层发布仍区分 mock 与 real。

## 真实设备演练

前置条件：

- 只在明确 `bms.device.mode=real` 时执行。
- 启动、停止、删除、发布、覆盖仍必须先进入 `TaskCommand` 和审批链路。
- 不允许直接调用设备适配器绕过审批。

设备离线演练：

```powershell
$env:BMS_DEVICE_MODE="real"
$env:BMS_DEVICE_CONNECT_TIMEOUT_MS="2000"
$env:BMS_DEVICE_RETRY_MAX_ATTEMPTS="1"
$env:BMS_DEVICE_CIRCUIT_FAILURE_THRESHOLD="2"
$env:BMS_DEVICE_CIRCUIT_OPEN_DURATION_MS="30000"
```

验收项：

- 设备 IP 不可达时返回 `SSH_IO_ERROR`、`HTTP_IO_ERROR` 或 `SSH_TIMEOUT`。
- 熔断打开后返回 `SSH_CIRCUIT_OPEN` 或 `HTTP_CIRCUIT_OPEN`。
- Attempt 日志保留失败分类、原始输出和耗时。
- 应用线程不因设备离线长期阻塞。

弱网演练：

- 将设备命令超时下调到 2 秒。
- 对 GeoServer 或设备端口注入延迟、断连或拒绝连接。
- 连续失败后确认熔断打开。
- 恢复网络后确认熔断窗口结束后可再次尝试。

## 压测记录模板

| 项目 | 结果 |
| --- | --- |
| 测试时间 |  |
| profile | mock / local / local,agent-local |
| 并发数 |  |
| 持续时间 |  |
| 目标接口 |  |
| P95 延迟 |  |
| 错误率 |  |
| 是否触发限流 |  |
| CPU / 内存 |  |
| 外部依赖状态 |  |

## 未验收不得标记完成

- 未启动真实 GeoServer，不得宣称 GeoServer 熔断已真实验收。
- 未设置 `bms.device.mode=real` 并执行 dry-run，不得宣称真实设备弱网和离线演练已完成。
- 未采集 Prometheus 指标，不得宣称监控闭环完成。

## 2026-05-21 本地验收记录

验收环境：

- 应用：`local` profile，`http://localhost:8088/v2`。
- Docker 依赖：PostgreSQL/PostGIS/pgvector、Redis、Kafka、MinIO、GeoServer、Prometheus、Grafana、Ollama。
- 自动化脚本：`scripts/verify-3.12-local.ps1`。

验收结论：通过。

| 验收项 | 结果 | 记录 |
| --- | --- | --- |
| Docker 本地依赖 | 成功 | `docker compose -f deploy/docker-compose.local.yml ps` 显示核心容器运行，Prometheus/Grafana 为 healthy。 |
| 应用健康检查 | 成功 | `/v2/actuator/health` 返回 `UP`。 |
| JWT 登录 | 成功 | `/v2/auth/token` 返回 Bearer token。 |
| RBAC 未鉴权访问 | 成功 | 未携带 token 访问业务接口返回统一 `code=401`。 |
| API 审计日志 | 成功 | 应用日志出现 `api_audit requestId=... method=... path=... principal=... durationMs=...`。 |
| API 限流 | 成功 | 140 次连续请求中触发 `code=429`，首次限流出现在阈值之后。 |
| PostgreSQL/PostGIS/pgvector | 成功 | `pg_isready` 可用，`postgis` 和 `vector` 扩展存在。 |
| Redis | 成功 | `redis-cli ping` 返回 `PONG`。 |
| Kafka | 成功 | `task-events`、`device-commands`、`asset-events`、`uav-telemetry`、`agent-events`、`dead-letter-events` topic 存在；真实 broker 集成测试通过。 |
| MinIO | 成功 | `/minio/health/live` 返回 200，并完成 `validation/3.12.txt` 写入和读取。 |
| GeoServer REST/WMS | 成功 | 通过应用创建 Mission/Task/Asset，计算 geo-boundary 后发布 `zhongyan:asset_geometry` 图层；GeoServer layer REST 和 WMS 请求均返回 200。 |
| GeoServer 超时/重试/熔断边界 | 成功 | 单元测试覆盖网络异常重试和熔断边界；真实 GeoServer 正常发布链路通过。 |
| Prometheus | 成功 | `zhongyan-uav-backend` target 为 `up`，可查询 `up=1`。 |
| Grafana | 成功 | `/api/health` 返回 database `ok`，Prometheus 数据源可通过 Grafana proxy 查询后端 `up` 指标。 |
| 真实模型服务 chat | 成功 | Ollama OpenAI 兼容 `/v1/chat/completions`，`qwen3:8b` 返回内容。 |
| 真实模型服务 embedding | 成功 | Ollama OpenAI 兼容 `/v1/embeddings`，`qwen3-embedding:0.6b` 返回 1024 维向量。 |
| HTTP 设备离线/熔断演练 | 成功 | `RealDeviceAdapterTests` 覆盖离线 HTTP 设备返回 `HTTP_IO_ERROR`，连续失败后返回 `HTTP_CIRCUIT_OPEN`。 |
| SSH 设备失败分类 | 成功 | `RealDeviceAdapterTests` 覆盖缺少命令时返回 `SSH_BAD_REQUEST`；代码路径包含 `SSH_IO_ERROR`、`SSH_INTERRUPTED`、`SSH_TIMEOUT`、`SSH_CIRCUIT_OPEN` 分类。 |
| 物理真实设备高风险动作 | 未执行 | 启动、停止、删除、发布、覆盖等动作仍必须通过 TaskCommand 和审批链路；本次未直接执行物理设备高风险命令。 |

本次执行命令：

```powershell
powershell -ExecutionPolicy Bypass -File scripts\verify-3.12-local.ps1
.\mvnw.cmd -Dtest=RealDeviceAdapterTests test
$env:BMS_REPOSITORY_ADAPTER_TESTS='true'
$env:BMS_KAFKA_INTEGRATION_TESTS='true'
$env:BMS_TELEMETRY_KAFKA_INTEGRATION_TESTS='true'
$env:BMS_TELEMETRY_LOCAL_E2E_TESTS='true'
.\mvnw.cmd test
```

执行结果：

- `scripts\verify-3.12-local.ps1`：`status=ok`，Prometheus target `up`，Grafana `11.2.0`。
- `.\mvnw.cmd -Dtest=RealDeviceAdapterTests test`：4 个测试通过。
- 带真实依赖开关的 `.\mvnw.cmd test`：93 个测试执行，0 failures，0 errors，1 skipped。

保留说明：

- 本次已经完成本地真实依赖闭环验收。
- 物理真实设备高风险动作未直接执行，原因是项目约束要求此类动作必须进入 TaskCommand 和审批链路；验收只覆盖自动化离线/熔断保护。
- 如需生产验收，需在生产等价网络、真实设备和正式凭据下重复执行本节脚本和人工审批链路检查。
